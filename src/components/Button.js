import styled from "styled-components";

const Button = styled.button`
  height: 60px;
  position: relative;
  display: flex;
  align-items: center;
  border: none;
  cursor: pointer;
  background: none;
`;

export default Button;
